i32 fibtail(i32 n, i32 a, i32 b) {
    if n == 0
        return a
    if n == 1
        return b

    return fibtail(n - 1, b, a + b)
}

i32 fibonacci(i32 n) {
    if n < 2
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)
}