use Console

struct Node {
    str Name,
    Node Next
}

fn main {
    Node node = Node {
        Name: "Node1",
        Next: Node {
            Name: "Node2",
            Next: Node {
                Name: "Node3",
                Next: Node {}
            }
        }
    }

    printl(node.Next.Next.Name)
}