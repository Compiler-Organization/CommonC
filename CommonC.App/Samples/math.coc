fn Sieve(i32 n) {
	bool nonprimes = bool[n] {}
	i32 sum = 2
	i32 prime = 3

	while (prime < n) {
		if (!nonprimes[prime]) {
			sum += prime
			i32 i = prime * prime
			while (i < n) {
				nonprimes[i] = true
				i += 2 * prime
			}
		}
		prime += 2
	}

	logl("Sieve sum: ", sum)
}